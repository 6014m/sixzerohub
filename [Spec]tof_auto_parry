-- Auto Parry — self-tuning detection
-- 5 detection methods + adaptive sensitivity from missfires/misses

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local HttpService = game:GetService("HttpService")
local lp = Players.LocalPlayer
local playerGui = lp:WaitForChild("PlayerGui")

local PUSH_ANIM_ID = "rbxassetid://134933487900882"
local SETTINGS_FILE = "AutoParry_sensitivity.json"

-- Adaptive sensitivity (saved to file)
local sensitivity = {
	range = 20,
	rangeApproach = 30,
	closeRange = 5,
	dirThreshold = 0, -- 0 = facing toward, negative = even back-turned
	missfires = 0,
	hits = 0,
	misses = 0,
}

-- Load saved sensitivity
pcall(function()
	if isfile and isfile(SETTINGS_FILE) then
		local saved = HttpService:JSONDecode(readfile(SETTINGS_FILE))
		for k, v in pairs(saved) do
			if sensitivity[k] ~= nil then sensitivity[k] = v end
		end
	end
end)

local function saveSensitivity()
	pcall(function()
		if writefile then
			writefile(SETTINGS_FILE, HttpService:JSONEncode(sensitivity))
		end
	end)
end

-- Tuning: tighten on REPEATED missfires, loosen on miss
local consecutiveMissfires = 0

local function onMissfire()
	sensitivity.missfires = sensitivity.missfires + 1
	consecutiveMissfires = consecutiveMissfires + 1
	-- Only tighten after 3+ consecutive missfires (one-off could be legit)
	if consecutiveMissfires >= 3 then
		sensitivity.range = math.max(8, sensitivity.range - 0.5)
		sensitivity.rangeApproach = math.max(12, sensitivity.rangeApproach - 0.5)
		sensitivity.dirThreshold = math.min(0.5, sensitivity.dirThreshold + 0.02)
		consecutiveMissfires = 0
		saveSensitivity()
	end
	if sensLabel then
		sensLabel.Text = "Missfire " .. consecutiveMissfires .. "/3 | R:" .. math.floor(sensitivity.range)
		sensLabel.TextColor3 = Color3.fromRGB(255, 200, 50)
	end
end

local function onConfirmedHit()
	sensitivity.hits = sensitivity.hits + 1
	consecutiveMissfires = 0 -- reset missfire streak on confirmed hit
	saveSensitivity()
	if sensLabel then
		sensLabel.Text = "Parried! | R:" .. math.floor(sensitivity.range) .. " H:" .. sensitivity.hits
		sensLabel.TextColor3 = Color3.fromRGB(87, 242, 135)
	end
end

local function onMiss()
	sensitivity.misses = sensitivity.misses + 1
	consecutiveMissfires = 0
	-- Misses are definitive — loosen aggressively
	sensitivity.range = math.min(35, sensitivity.range + 2)
	sensitivity.rangeApproach = math.min(45, sensitivity.rangeApproach + 3)
	sensitivity.dirThreshold = math.max(-0.3, sensitivity.dirThreshold - 0.1)
	sensitivity.closeRange = math.min(10, sensitivity.closeRange + 0.5)
	saveSensitivity()
	if sensLabel then
		sensLabel.Text = "MISSED! Loosening | R:" .. math.floor(sensitivity.range)
		sensLabel.TextColor3 = Color3.fromRGB(255, 100, 100)
	end
end

local connections = {}
local recentParry = 0
local parryPending = false -- true = we parried, waiting to see if it was legit

---------- Pre-cached refs (updated constantly) ----------

local myChar, myHRP, myHum, myBackpack, parryTool

local function cacheLocal()
	myChar = lp.Character
	myHRP = myChar and myChar:FindFirstChild("HumanoidRootPart")
	myHum = myChar and myChar:FindFirstChildOfClass("Humanoid")
	myBackpack = lp:FindFirstChild("Backpack")
	parryTool = myBackpack and myBackpack:FindFirstChild("Parry")
end

cacheLocal()
lp.CharacterAdded:Connect(function() task.wait(0.2); cacheLocal() end)

-- Refresh parry tool ref constantly since it can appear/disappear
task.spawn(function()
	while true do
		myBackpack = lp:FindFirstChild("Backpack")
		parryTool = myBackpack and myBackpack:FindFirstChild("Parry")
		task.wait(0.3)
	end
end)

---------- Player cache ----------

local playerCache = {}
local playerHRPCache = {} -- [player] = HumanoidRootPart (refreshed each frame)

local function rebuildPlayerCache()
	playerCache = {}
	for _, p in ipairs(Players:GetPlayers()) do
		if p ~= lp then playerCache[#playerCache+1] = p end
	end
end

rebuildPlayerCache()
Players.PlayerAdded:Connect(rebuildPlayerCache)
Players.PlayerRemoving:Connect(rebuildPlayerCache)

---------- Horizontal Density Burst ----------

local hdActive = false

local function burstHorizontalDensity()
	if hdActive or not myHRP or not myHum then return end
	hdActive = true

	local att = Instance.new("Attachment")
	att.Name = "HDParryAttach"
	att.Parent = myHRP

	local linVel = Instance.new("LinearVelocity")
	linVel.Name = "HDParryForce"
	linVel.Attachment0 = att
	linVel.VelocityConstraintMode = Enum.VelocityConstraintMode.Plane
	linVel.PrimaryTangentAxis = Vector3.new(1, 0, 0)
	linVel.SecondaryTangentAxis = Vector3.new(0, 0, 1)
	linVel.RelativeTo = Enum.ActuatorRelativeTo.World
	linVel.ForceLimitMode = Enum.ForceLimitMode.Magnitude
	linVel.MaxForce = math.huge
	linVel.PlaneVelocity = Vector2.new(0, 0)
	linVel.Parent = myHRP

	local hdConn
	hdConn = RunService.Heartbeat:Connect(function()
		if not myHRP or not myHum or not myHRP.Parent then return end
		local moveDir = myHum.MoveDirection
		local ws = myHum.WalkSpeed
		linVel.PlaneVelocity = Vector2.new(moveDir.X * ws, moveDir.Z * ws)
		local vel = myHRP.AssemblyLinearVelocity
		myHRP.AssemblyLinearVelocity = Vector3.new(
			moveDir.X * ws, vel.Y, moveDir.Z * ws
		)
	end)

	task.delay(1, function()
		hdConn:Disconnect()
		pcall(function() linVel:Destroy() end)
		pcall(function() att:Destroy() end)
		hdActive = false
	end)
end

---------- Core parry (as fast as possible) ----------

local function fireParry()
	if not parryTool or not myHum then
		cacheLocal()
		if not parryTool or not myHum then return false end
	end
	myHum:EquipTool(parryTool)
	parryTool:Activate()
	task.delay(0.15, function()
		pcall(function() myHum:UnequipTools() end)
	end)
	return true
end

-- Inline range/direction check — no function call overhead
local function shouldParry(theirHRP)
	if not myHRP then return false end
	-- Skip ragdolled players — they can't push
	local theirHum = theirHRP.Parent and theirHRP.Parent:FindFirstChildOfClass("Humanoid")
	if theirHum then
		local state = theirHum:GetState()
		if state == Enum.HumanoidStateType.Ragdoll or state == Enum.HumanoidStateType.FallingDown then
			return false
		end
	end
	local ox = myHRP.Position.X - theirHRP.Position.X
	local oy = myHRP.Position.Y - theirHRP.Position.Y
	local oz = myHRP.Position.Z - theirHRP.Position.Z
	local distSq = ox*ox + oy*oy + oz*oz

	if distSq <= sensitivity.closeRange * sensitivity.closeRange then return true end

	local vel = theirHRP.AssemblyLinearVelocity
	local vMag = vel.Magnitude
	local maxRange = sensitivity.range
	if vMag > 1 then
		local dist = math.sqrt(distSq)
		local dirX, dirZ = ox / dist, oz / dist
		local dot = vel.X * dirX + vel.Z * dirZ
		if dot > 0.3 * vMag then
			maxRange = sensitivity.rangeApproach
		end
	end

	if distSq > maxRange * maxRange then return false end

	local look = theirHRP.CFrame.LookVector
	local dist = math.sqrt(distSq)
	return (look.X * ox + look.Y * oy + look.Z * oz) / dist >= sensitivity.dirThreshold
end

local function doParry(player, theirHRP)
	local now = tick()
	if now - recentParry < 0.5 then return end
	if not shouldParry(theirHRP) then return end
	recentParry = now
	burstHorizontalDensity()
	if fireParry() then
		flashLabel.Text = "PARRIED " .. player.Name
		flashLabel.Visible = true
		task.delay(1.5, function() flashLabel.Visible = false end)

		-- Track: was this parry legit or a missfire?
		parryPending = true
		task.delay(2, function()
			if parryPending then
				-- No push confirmed within 2s = missfire
				parryPending = false
				onMissfire()
			end
		end)
	end
end

---------- Notify ----------

local gui = Instance.new("ScreenGui")
gui.Name = "AutoParry"
gui.ResetOnSpawn = false
gui.Parent = playerGui

flashLabel = Instance.new("TextLabel")
flashLabel.Size = UDim2.new(0, 200, 0, 30)
flashLabel.Position = UDim2.new(0.5, -100, 0, 50)
flashLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 35)
flashLabel.BackgroundTransparency = 0.1
flashLabel.BorderSizePixel = 0
flashLabel.TextColor3 = Color3.fromRGB(87, 242, 135)
flashLabel.Font = Enum.Font.GothamBold
flashLabel.TextSize = 14
flashLabel.Visible = false
flashLabel.Parent = gui
Instance.new("UICorner", flashLabel).CornerRadius = UDim.new(0, 6)

sensLabel = Instance.new("TextLabel")
sensLabel.Size = UDim2.new(0, 200, 0, 18)
sensLabel.Position = UDim2.new(0.5, -100, 0, 82)
sensLabel.BackgroundTransparency = 1
sensLabel.Text = "R:" .. math.floor(sensitivity.range) .. " A:" .. math.floor(sensitivity.rangeApproach) .. " H:" .. sensitivity.hits .. " M:" .. sensitivity.misses .. " MF:" .. sensitivity.missfires
sensLabel.TextColor3 = Color3.fromRGB(150, 150, 150)
sensLabel.Font = Enum.Font.Code
sensLabel.TextSize = 9
sensLabel.Visible = true
sensLabel.Parent = gui

---------- METHOD 1: AnimationPlayed (catches push windup) ----------

local function watchAnimations(player, char)
	local hum = char:FindFirstChildOfClass("Humanoid")
	if not hum then return end
	local animator = hum:FindFirstChildOfClass("Animator")
	if not animator then animator = hum:WaitForChild("Animator", 3) end
	if not animator then return end

	local c = animator.AnimationPlayed:Connect(function(track)
		local anim = track.Animation
		if not anim then return end

		-- Match exact push animation ID
		if anim.AnimationId == PUSH_ANIM_ID then
			local hrp = char:FindFirstChild("HumanoidRootPart")
			if hrp then doParry(player, hrp) end
			return
		end

		-- Also trigger if they have Push equipped and ANY animation plays
		-- (catches animation ID changes / variants)
		if char:FindFirstChild("Push") then
			local hrp = char:FindFirstChild("HumanoidRootPart")
			if hrp then doParry(player, hrp) end
		end
	end)
	table.insert(connections, c)
end

---------- METHOD 2: Tool equip (Push enters Character) ----------

local function watchToolEquip(player, char)
	local c = char.ChildAdded:Connect(function(child)
		if child:IsA("Tool") and child.Name == "Push" then
			local hrp = char:FindFirstChild("HumanoidRootPart")
			if hrp then doParry(player, hrp) end
		end
	end)
	table.insert(connections, c)
end

---------- METHOD 3: Backpack ChildRemoved (Push leaves Backpack = equipped) ----------
-- This can fire BEFORE ChildAdded on Character due to replication order

local function watchBackpack(player)
	local function hookBackpack(backpack)
		local c = backpack.ChildRemoved:Connect(function(child)
			if child.Name == "Push" then
				local char = player.Character
				local hrp = char and char:FindFirstChild("HumanoidRootPart")
				if hrp then doParry(player, hrp) end
			end
		end)
		table.insert(connections, c)
	end

	local bp = player:FindFirstChild("Backpack")
	if bp then hookBackpack(bp) end

	-- Backpack gets recreated on respawn
	local c = player.ChildAdded:Connect(function(child)
		if child.Name == "Backpack" then hookBackpack(child) end
	end)
	table.insert(connections, c)
end

---------- Watch all players ----------

local function watchCharacter(player, char)
	watchAnimations(player, char)
	watchToolEquip(player, char)
end

local function watchPlayer(player)
	if player == lp then return end
	if player.Character then watchCharacter(player, player.Character) end
	watchBackpack(player)
	local c = player.CharacterAdded:Connect(function(char)
		task.wait(0.2)
		watchCharacter(player, char)
	end)
	table.insert(connections, c)
end

for _, p in ipairs(Players:GetPlayers()) do watchPlayer(p) end
Players.PlayerAdded:Connect(watchPlayer)

---------- METHOD 4: RenderStepped scan (earliest in frame pipeline) ----------

RunService.RenderStepped:Connect(function()
	if tick() - recentParry < 0.5 then return end
	if not myHRP then cacheLocal(); return end

	local myPos = myHRP.Position

	for i = 1, #playerCache do
		local p = playerCache[i]
		local char = p.Character
		if char then
			local hrp = char:FindFirstChild("HumanoidRootPart")
			if hrp then
				-- Quick squared distance pre-check before expensive FindFirstChild
				local dx = myPos.X - hrp.Position.X
				local dz = myPos.Z - hrp.Position.Z
				local distSq = dx*dx + dz*dz
				if distSq < RANGE_APPROACHING * RANGE_APPROACHING then
					local push = char:FindFirstChild("Push")
					if push and push:IsA("Tool") then
						doParry(p, hrp)
						return
					end
				end
			end
		end
	end
end)

---------- METHOD 5: Heartbeat backup (physics step, different timing) ----------

RunService.Heartbeat:Connect(function()
	if tick() - recentParry < 0.5 then return end
	if not myHRP then return end

	local myPos = myHRP.Position

	for i = 1, #playerCache do
		local p = playerCache[i]
		local char = p.Character
		if char then
			local hrp = char:FindFirstChild("HumanoidRootPart")
			if hrp then
				local dx = myPos.X - hrp.Position.X
				local dz = myPos.Z - hrp.Position.Z
				if dx*dx + dz*dz < RANGE_APPROACHING * RANGE_APPROACHING then
					local push = char:FindFirstChild("Push")
					if push and push:IsA("Tool") then
						doParry(p, hrp)
						return
					end
				end
			end
		end
	end
end)

---------- FEEDBACK: PushStuff confirms hit / miss ----------

local function isMe(name)
	return name == lp.Name or name == lp.DisplayName or (myChar and name == myChar.Name)
end

local function watchPushStuffFeedback(pushStuff)
	local c = pushStuff.ChildAdded:Connect(function(child)
		if isMe(child.Name) then
			-- We got pushed
			if parryPending then
				-- We parried AND got pushed = confirmed parry was correct timing
				parryPending = false
				onConfirmedHit()
			else
				-- We got pushed WITHOUT parrying = we missed it
				onMiss()
			end
		end
	end)
	table.insert(connections, c)

	local c2 = pushStuff.DescendantAdded:Connect(function(desc)
		local current = desc
		for i = 1, 4 do
			if not current then break end
			if isMe(current.Name) then
				if parryPending then
					parryPending = false
					onConfirmedHit()
				else
					onMiss()
				end
				return
			end
			current = current.Parent
		end
	end)
	table.insert(connections, c2)
end

local ps = workspace:FindFirstChild("PushStuff")
if ps then watchPushStuffFeedback(ps) end
table.insert(connections, workspace.ChildAdded:Connect(function(child)
	if child.Name == "PushStuff" then watchPushStuffFeedback(child) end
end))

---------- FEEDBACK: Ragdoll detection ----------
-- Only counts as a miss if we ragdoll AND we didn't recently parry
-- (ragdoll after parry = they got parried, that's a success)

local function hookRagdoll(char)
	local hum = char:WaitForChild("Humanoid", 5)
	if not hum then return end
	local c = hum.StateChanged:Connect(function(_, newState)
		if newState == Enum.HumanoidStateType.Ragdoll
			or newState == Enum.HumanoidStateType.FallingDown then
			-- Only a miss if we haven't parried in the last 3 seconds
			-- (ragdoll right after parry = successful parry on them)
			if tick() - recentParry > 3 then
				onMiss()
			end
		end
	end)
	table.insert(connections, c)
end

if myChar then hookRagdoll(myChar) end
lp.CharacterAdded:Connect(function(char)
	task.wait(0.5)
	hookRagdoll(char)
end)

---------- Auto Delete Ropes ----------

local function isAttachedToMe(rope)
	if not myChar then return false end
	local a0, a1 = rope.Attachment0, rope.Attachment1
	return (a0 and a0:IsDescendantOf(myChar)) or (a1 and a1:IsDescendantOf(myChar))
end

local function checkRope(obj)
	if obj:IsA("RopeConstraint") or obj:IsA("RodConstraint") then
		pcall(function() if isAttachedToMe(obj) then obj:Destroy() end end)
	end
end

for _, v in ipairs(workspace:GetDescendants()) do checkRope(v) end
workspace.DescendantAdded:Connect(checkRope)

-- SixZeroHub cleanup
if _G.SixZeroHubRegister then
	_G.SixZeroHubRegister("Auto Parry", "[Spec]tof_auto_parry", function()
		for _, c in ipairs(connections) do pcall(function() c:Disconnect() end) end
		pcall(function() gui:Destroy() end)
	end, "spec")
end
