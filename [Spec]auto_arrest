-- [Spec] Auto Arrest
-- IY-style tween, click arrest, reset after confirmed arrest
-- Priority: keycard holders > criminals > inmates (only if in restricted area)
-- Criminals are always arrestable, inmates only sometimes

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local VIM = game:GetService("VirtualInputManager")

local player = Players.LocalPlayer
local connections = {}
local active = true
local tweenSpeed = -1

-- inmates who failed arrest are probably in a safe zone, skip them temporarily
local skipList = {} -- [playerName] = os.clock() when we gave up

local function equipHandcuffs()
    pcall(function()
        local char = player.Character
        if not char then return end
        local held = char:FindFirstChildOfClass("Tool")
        if held and held.Name:lower():find("cuff") then return end
        local backpack = player:FindFirstChild("Backpack")
        if backpack then
            for _, tool in ipairs(backpack:GetChildren()) do
                if tool:IsA("Tool") and tool.Name:lower():find("cuff") then
                    tool.Parent = char
                    return
                end
            end
        end
    end)
end

local function getTeamName(plr)
    local name = ""
    pcall(function() name = tostring(plr.Team and plr.Team.Name or ""):lower() end)
    return name
end

local function isCriminal(plr)
    return getTeamName(plr):find("criminal") ~= nil
end

local function isInmate(plr)
    local team = getTeamName(plr)
    return (team:find("inmate") or team:find("prisoner")) ~= nil
end

local function hasKeycard(plr)
    if not plr.Character then return false end
    for _, child in ipairs(plr.Character:GetChildren()) do
        if child:IsA("Tool") and child.Name:lower():find("key") then return true end
    end
    if plr:FindFirstChild("Backpack") then
        for _, child in ipairs(plr.Backpack:GetChildren()) do
            if child:IsA("Tool") and child.Name:lower():find("key") then return true end
        end
    end
    return false
end

local function hasWeapon(plr)
    if not plr.Character then return false end
    for _, child in ipairs(plr.Character:GetChildren()) do
        if child:IsA("Tool") then
            local n = child.Name:lower()
            if n:find("gun") or n:find("knife") or n:find("ak") or n:find("m4") or n:find("pistol") or n:find("shotgun") or n:find("remington") then
                return true
            end
        end
    end
    return false
end

local function getPriority(plr)
    if plr == player then return nil end
    if not plr.Character then return nil end

    local criminal = isCriminal(plr)
    local inmate = isInmate(plr)
    if not criminal and not inmate then return nil end

    -- skip inmates we recently failed to arrest (probably in safe zone)
    if inmate and not criminal then
        local skipTime = skipList[plr.Name]
        if skipTime and os.clock() - skipTime < 15 then
            return nil
        end
    end

    if hasKeycard(plr) then return 1 end
    if criminal then return 2 end
    -- inmates with weapons are more likely in restricted area
    if hasWeapon(plr) then return 3 end
    return 4
end

local function getRoot(char)
    if not char then return nil end
    return char:FindFirstChild("HumanoidRootPart")
end

local function getTorso(plr)
    if not plr.Character then return nil end
    return plr.Character:FindFirstChild("Torso") or plr.Character:FindFirstChild("UpperTorso")
end

-- check if player got arrested: team changed OR arrest gui/timer appeared on them
local function isArrested(plr)
    if not plr.Parent then return true end
    -- team changed off criminal/inmate
    local team = getTeamName(plr)
    if not (team:find("criminal") or team:find("inmate") or team:find("prisoner")) then
        return true
    end
    -- check for arrest timer/billboard gui on their character
    if plr.Character then
        local head = plr.Character:FindFirstChild("Head")
        if head then
            for _, child in ipairs(head:GetChildren()) do
                if child:IsA("BillboardGui") then
                    -- arrest timer gui appeared
                    return true
                end
            end
        end
        -- also check character root
        for _, child in ipairs(plr.Character:GetChildren()) do
            if child:IsA("BillboardGui") and child.Name ~= "NameTag" then
                return true
            end
        end
    end
    return false
end

-- IY exact tween
local function tweenTo(targetRoot)
    local root = getRoot(player.Character)
    if not root or not targetRoot or not targetRoot.Parent then return end

    pcall(function()
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if hum and hum.SeatPart then
            hum.Sit = false
            task.wait(0.1)
        end
    end)

    local tween = TweenService:Create(
        root,
        TweenInfo.new(tweenSpeed, Enum.EasingStyle.Linear),
        {CFrame = targetRoot.CFrame + Vector3.new(3, 1, 0)}
    )
    tween:Play()
    tween.Completed:Wait()

    pcall(function()
        root.Velocity = Vector3.new(0, 0, 0)
        root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        root.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
    end)
end

local function clickAt(x, y)
    -- VirtualInputManager: real mouse emulation
    pcall(function()
        VIM:SendMouseButtonEvent(x, y, 0, true, game, 1)
        task.wait()
        VIM:SendMouseButtonEvent(x, y, 0, false, game, 1)
    end)
    -- mousemoveabs + mouse1click backup
    pcall(function()
        if mousemoveabs and mouse1click then
            mousemoveabs(x, y)
            task.wait()
            mouse1click()
        end
    end)
end

local function arrestTarget(targetPlr)
    local torso = getTorso(targetPlr)
    if not torso then return end

    local cam = workspace.CurrentCamera
    local screenPos, onScreen = cam:WorldToScreenPoint(torso.Position)
    if not onScreen then return end

    clickAt(screenPos.X, screenPos.Y + 36)
end

local function resetCharacter()
    pcall(function()
        local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = 0 end
    end)
    local ok, newChar = pcall(function()
        return player.CharacterAdded:Wait()
    end)
    if ok and newChar then
        newChar:WaitForChild("HumanoidRootPart", 5)
        task.wait(0.5)
    end
end

-- main loop
task.spawn(function()
    while active do
        local targets = {}
        for _, plr in ipairs(Players:GetPlayers()) do
            local pri = getPriority(plr)
            if pri then
                local root = getRoot(plr.Character)
                if root then
                    table.insert(targets, { player = plr, root = root, priority = pri })
                end
            end
        end

        table.sort(targets, function(a, b) return a.priority < b.priority end)

        if #targets == 0 then
            task.wait(1)
            continue
        end

        equipHandcuffs()

        for _, target in ipairs(targets) do
            if not active then break end
            if not target.player.Parent then continue end
            if not target.root or not target.root.Parent then continue end
            if not getPriority(target.player) then continue end

            local isCrim = isCriminal(target.player)
            -- criminals get more retries since they're always arrestable
            -- inmates get fewer since they might be in a safe zone
            local maxAttempts = isCrim and 15 or 3

            local arrested = false
            for attempt = 1, maxAttempts do
                if not active then break end
                if isArrested(target.player) then
                    arrested = true
                    break
                end

                local freshRoot = getRoot(target.player.Character)
                if not freshRoot or not freshRoot.Parent then break end

                tweenTo(freshRoot)
                task.wait(0.05)

                arrestTarget(target.player)

                -- quick check
                task.wait(0.3)
                if isArrested(target.player) then
                    arrested = true
                    break
                end
            end

            if arrested then
                -- confirmed arrest, reset to clear cooldown
                resetCharacter()
                equipHandcuffs()
            elseif not isCrim then
                -- inmate couldn't be arrested, probably in safe zone, skip for 15s
                skipList[target.player.Name] = os.clock()
            end
        end

        task.wait(0.5)
    end
end)

---------- SIXZEROHUB REGISTRATION ----------

local function cleanup()
    active = false
    for _, c in ipairs(connections) do pcall(function() c:Disconnect() end) end
    connections = {}
end

if _G.SixZeroHubRegister then
    _G.SixZeroHubRegister("Auto Arrest", "[Spec]auto_arrest", cleanup, "spec")
end
