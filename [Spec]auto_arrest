-- [Spec] Auto Arrest
-- CFrame desync: server sees you next to target, other players see you far away
-- Probe inmates with InvokeServer, only pursue if server confirms arrestable

local Players = game:GetService("Players")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local TweenService = game:GetService("TweenService")
local RunService = game:GetService("RunService")
local VIM = game:GetService("VirtualInputManager")

local player = Players.LocalPlayer
local connections = {}
local active = true
local tweenSpeed = -1

local skipList = {}
local SKIP_DURATION = 20

---------- CFRAME DESYNC ----------

local desyncEnabled = false
local realCFrame = nil -- where the server thinks we are (next to target)
local fakeCFrame = nil -- where we visually appear (far away)
local FAKE_OFFSET = Vector3.new(0, 0, 9999) -- visual body way off in the distance

-- Stepped runs before physics replication: set REAL position so server sees it
local steppedConn = RunService.Stepped:Connect(function()
    if not active or not desyncEnabled or not realCFrame then return end
    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    root.CFrame = realCFrame
end)
table.insert(connections, steppedConn)

-- RenderStepped runs before render: offset to FAKE position so others see it
local renderConn = RunService.RenderStepped:Connect(function()
    if not active or not desyncEnabled or not realCFrame then return end
    local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
    if not root then return end
    root.CFrame = realCFrame + FAKE_OFFSET
end)
table.insert(connections, renderConn)

local function startDesync(targetCFrame)
    realCFrame = targetCFrame
    desyncEnabled = true
end

local function stopDesync()
    desyncEnabled = false
    -- snap back to real position
    pcall(function()
        local root = player.Character and player.Character:FindFirstChild("HumanoidRootPart")
        if root and realCFrame then
            root.CFrame = realCFrame
        end
    end)
    realCFrame = nil
end

---------- TOOLS ----------

local function equipHandcuffs()
    pcall(function()
        local char = player.Character
        if not char then return end
        local held = char:FindFirstChildOfClass("Tool")
        if held and held.Name:lower():find("cuff") then return end
        local backpack = player:FindFirstChild("Backpack")
        if backpack then
            for _, tool in ipairs(backpack:GetChildren()) do
                if tool:IsA("Tool") and tool.Name:lower():find("cuff") then
                    tool.Parent = char
                    return
                end
            end
        end
    end)
end

---------- PLAYER CHECKS ----------

local function getTeamName(plr)
    local name = ""
    pcall(function() name = tostring(plr.Team and plr.Team.Name or ""):lower() end)
    return name
end

local function isCriminal(plr)
    return getTeamName(plr):find("criminal") ~= nil
end

local function isInmate(plr)
    local team = getTeamName(plr)
    return (team:find("inmate") or team:find("prisoner")) ~= nil
end

local function hasKeycard(plr)
    if not plr.Character then return false end
    for _, child in ipairs(plr.Character:GetChildren()) do
        if child:IsA("Tool") and child.Name:lower():find("key") then return true end
    end
    if plr:FindFirstChild("Backpack") then
        for _, child in ipairs(plr.Backpack:GetChildren()) do
            if child:IsA("Tool") and child.Name:lower():find("key") then return true end
        end
    end
    return false
end

local function isSkipped(plr)
    local t = skipList[plr.Name]
    return t and os.clock() - t < SKIP_DURATION
end

local function isArrested(plr)
    if not plr.Parent then return true end
    local team = getTeamName(plr)
    if not (team:find("criminal") or team:find("inmate") or team:find("prisoner")) then
        return true
    end
    if plr.Character then
        local head = plr.Character:FindFirstChild("Head")
        if head then
            for _, child in ipairs(head:GetChildren()) do
                if child:IsA("BillboardGui") then return true end
            end
        end
        for _, child in ipairs(plr.Character:GetChildren()) do
            if child:IsA("BillboardGui") and child.Name ~= "NameTag" then
                return true
            end
        end
    end
    return false
end

---------- MOVEMENT ----------

local function getRoot(char)
    if not char then return nil end
    return char:FindFirstChild("HumanoidRootPart")
end

local function getTorso(plr)
    if not plr.Character then return nil end
    return plr.Character:FindFirstChild("Torso") or plr.Character:FindFirstChild("UpperTorso")
end

-- move real position next to target (desync handles the visual)
local function moveToTarget(targetRoot)
    if not targetRoot or not targetRoot.Parent then return end
    realCFrame = targetRoot.CFrame + Vector3.new(3, 0, 0)
end

-- IY tween for when desync is off (initial approach)
local function tweenTo(targetRoot)
    local root = getRoot(player.Character)
    if not root or not targetRoot or not targetRoot.Parent then return end

    pcall(function()
        local hum = player.Character:FindFirstChildOfClass("Humanoid")
        if hum and hum.SeatPart then
            hum.Sit = false
            task.wait(0.1)
        end
    end)

    local tween = TweenService:Create(
        root,
        TweenInfo.new(tweenSpeed, Enum.EasingStyle.Linear),
        {CFrame = targetRoot.CFrame + Vector3.new(3, 0, 0)}
    )
    tween:Play()
    tween.Completed:Wait()

    pcall(function()
        root.Velocity = Vector3.new(0, 0, 0)
        root.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
        root.AssemblyAngularVelocity = Vector3.new(0, 0, 0)
    end)
end

---------- ARREST ----------

local function clickOnPlayer(plr)
    local torso = getTorso(plr)
    if not torso then return end
    local cam = workspace.CurrentCamera
    local screenPos, onScreen = cam:WorldToScreenPoint(torso.Position)
    if not onScreen then return end
    pcall(function()
        VIM:SendMouseButtonEvent(screenPos.X, screenPos.Y + 36, 0, true, game, 1)
        task.wait()
        VIM:SendMouseButtonEvent(screenPos.X, screenPos.Y + 36, 0, false, game, 1)
    end)
    pcall(function()
        if mousemoveabs and mouse1click then
            mousemoveabs(screenPos.X, screenPos.Y + 36)
            task.wait()
            mouse1click()
        end
    end)
end

local function fireArrestRemote(plr)
    pcall(function()
        local torso = getTorso(plr)
        if torso then
            ReplicatedStorage.Remotes.InteractWithItem:InvokeServer(torso)
        end
    end)
end

local function tryArrestProbe(plr)
    fireArrestRemote(plr)
    task.wait(0.3)
    return isArrested(plr)
end

---------- RESET ----------

local function resetCharacter()
    stopDesync()
    pcall(function()
        local hum = player.Character and player.Character:FindFirstChildOfClass("Humanoid")
        if hum then hum.Health = 0 end
    end)
    local ok, newChar = pcall(function()
        return player.CharacterAdded:Wait()
    end)
    if ok and newChar then
        newChar:WaitForChild("HumanoidRootPart", 5)
        task.wait(0.5)
    end
end

---------- MAIN LOOP ----------

task.spawn(function()
    while active do
        local targets = {}
        for _, plr in ipairs(Players:GetPlayers()) do
            if plr == player or not plr.Character then continue end
            if isSkipped(plr) or isArrested(plr) then continue end

            local crim = isCriminal(plr)
            local inm = isInmate(plr)
            if not crim and not inm then continue end

            local root = getRoot(plr.Character)
            if not root then continue end

            if crim then
                local pri = hasKeycard(plr) and 1 or 2
                table.insert(targets, { player = plr, root = root, priority = pri, isCriminal = true })
            elseif inm then
                table.insert(targets, { player = plr, root = root, priority = 10, isCriminal = false })
            end
        end

        table.sort(targets, function(a, b) return a.priority < b.priority end)

        if #targets == 0 then
            stopDesync()
            task.wait(1)
            continue
        end

        equipHandcuffs()

        for _, target in ipairs(targets) do
            if not active then break end
            if not target.player.Parent then continue end
            if isArrested(target.player) then continue end

            -- for inmates: probe once to check if arrestable
            if not target.isCriminal then
                -- tween close first
                local freshRoot = getRoot(target.player.Character)
                if not freshRoot then continue end
                tweenTo(freshRoot)
                task.wait(0.1)

                if not tryArrestProbe(target.player) then
                    skipList[target.player.Name] = os.clock()
                    continue
                else
                    -- probe arrested them, reset cooldown
                    resetCharacter()
                    equipHandcuffs()
                    continue
                end
            end

            -- criminals: full desync pursuit
            -- first tween close, then enable desync
            local freshRoot = getRoot(target.player.Character)
            if not freshRoot then continue end

            tweenTo(freshRoot)
            task.wait(0.1)

            -- now enable desync: real pos stays near target, visual goes far away
            startDesync(freshRoot.CFrame + Vector3.new(3, 0, 0))

            local arrested = false
            for attempt = 1, 15 do
                if not active then break end
                if isArrested(target.player) then
                    arrested = true
                    break
                end

                -- update real position to follow moving target
                local updatedRoot = getRoot(target.player.Character)
                if not updatedRoot or not updatedRoot.Parent then break end
                moveToTarget(updatedRoot)

                task.wait(0.05)

                -- click + remote
                clickOnPlayer(target.player)
                task.wait(0.1)
                fireArrestRemote(target.player)

                task.wait(0.3)
                if isArrested(target.player) then
                    arrested = true
                    break
                end
            end

            stopDesync()

            if arrested then
                resetCharacter()
                equipHandcuffs()
            end
        end

        task.wait(0.5)
    end
end)

---------- SIXZEROHUB REGISTRATION ----------

local function cleanup()
    active = false
    stopDesync()
    for _, c in ipairs(connections) do pcall(function() c:Disconnect() end) end
    connections = {}
end

if _G.SixZeroHubRegister then
    _G.SixZeroHubRegister("Auto Arrest", "[Spec]auto_arrest", cleanup, "spec")
end
